[project]
name = "llm-eval-chapter1"
version = "0.1.0"
description = "Chapitre 1 - Setup Evaluation"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "pandas==3.0.6",
    "evidently==0.7.23",
]

[dependency-groups]
dev = []